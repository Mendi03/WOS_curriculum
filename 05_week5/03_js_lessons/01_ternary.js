// Create a variable temp = 75. Use a ternary to assign "hot" or "cold" to another variable based on temp > 70.
const degrees = 75;
const temperature = degrees > 70 ? "hot" : "cold";

console.log(temperature);

// 2 Use a ternary to return "even" or "odd" depending on whether a number is divisible by 2.
const number = 40;
const evenOrOdd = number % 2 === 0 ? "even" : "odd";

console.log(evenOrOdd);


// 3 In JSX, conditionally render a <span> with different text depending on whether a pizza is delivered.


