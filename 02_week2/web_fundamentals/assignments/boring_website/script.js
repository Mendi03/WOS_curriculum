console.log('all hooked up');

/**
 *
 * @param {HTMLElement} elem
 */
function hide(elem){
    elem.remove();
}


/**
 *
 * @param {HTMLElement} elem
 */
function lesgo(elem){
    elem.style.backgroundColor = "yellow";
    elem.style.color = "skyblue";
    elem.innerText = "Let's Goooooo!!"
}


/**
 *
 * @param {HTMLElement} elem
 */
function reset(elem){
    elem.style.backgroundColor = "rgb(23, 23, 23)"; 
    elem.style.color = "white";
    elem.innerText = "My Boring Website";
}

/**
 *
 * @param {HTMLElement} elem
 */
function facts(elem){
    elem.innerText = "Fun Fact";
}

/**
 *
 * @param {HTMLElement} elem
 */
function facts2(elem){
    elem.innerText = "Chewbacca in \"Star Wars\" is based on George Lucas dog, an Alaskan Malamute named \"Indiana,\" who also inspired \"Indiana Jones.\"";

}

/**
 *
 * @param {HTMLElement} elem
 */
function party(elem){
    elem.style.backgroundColor = "skyBlue";
}

/**
 *
 * @param {HTMLElement} elem
 */
function party2(elem){
    elem.style.color = "yellow";
    elem.innerText = "A Brilliant Azure Sky";
}

/**
 *
 * @param {HTMLElement} elem
 */
function party3(elem){
    elem.style.color = "white";
    elem.innerText = "Colossal, tempestuous clouds churned and roiled with an almost palpable energy. The sun, a blazing orb of pure power, scorched the very air, casting razor-sharp shadows that danced and writhed across the parched earth below. Every fiber of the ground vibrated with an unseen force, a primal pulse that resonated deep within.";
}

/**
 *
 * @param {HTMLElement} elem
 */
function party4(elem){
    elem.style.backgroundColor = "skyBlue";
}

/**
 *
 * @param {HTMLElement} elem
 */
function party5(elem){
    elem.style.backgroundColor = "skyBlue";
}

// emojis

function emotion1(elem){
    elem.innerText = "😆";
}

function emotion2(elem){
    elem.innerText = "🥳";
}

function emotion3(elem){
    elem.innerText = "🤪";
}

function unamused(elem){
    elem.innerText = "😐";
}


